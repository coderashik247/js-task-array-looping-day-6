// Use a for...of loop to concatenate all the elements of an array into a single string.

let numbers = ['Tom', 'Tim', 'Tin', 'Tik'];

let newNumbers = numbers.join('');

let oneString = [];

oneString.push(newNumbers);

console.log(oneString);